return {
	myNode = {
		type = "reliable", -- determines whether the node is a RemoteEvent or an UnreliableRemoteEvent (overrides `returns`)
		returns = false, -- determines whether the node is a RemoteEvent or RemoteFunction
		throttle = 1,
		paramCount = 1,
	}
}